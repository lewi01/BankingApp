/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package lewisCode.app;

import lewisCode.list.LinkedList;

import static lewisCode.utilities.StringUtils.join;
import static lewisCode.utilities.StringUtils.split;
import static lewisCode.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
